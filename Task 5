def search():
    A = input("Задати набір цифр: ")
    digits = [int(b) for b in A if b.isdigit()]
    if digits:
        m = max(digits.count(c) for c in set(digits))
        d = sorted(c for c in set(digits) if digits.count(c) == m)
        print("Цифри, яких найбільше в наборі:", d)
search()

def delete():
    A = list(map(int,input('Enter a list: ').split()))
    print(A)
    result = []
    for x in range(len(A)):
        if x % 2 != 0:
            result.append(A[x])
    print(result)
    return result
delete()
